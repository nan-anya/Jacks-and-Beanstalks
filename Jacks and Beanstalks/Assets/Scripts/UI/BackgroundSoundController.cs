using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundSoundController : MonoBehaviour
{
    // Scene에 따라 BGM을 재생하는 스크립트
    // AudioManager.BGMStart() 와 AudioManager.BGMStop 을 이용해서 상황에 따라 재생하면 된다.
}
